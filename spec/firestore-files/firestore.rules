rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // Allow ALL
    match /{document=**} {
      allow read: if true;
      allow write: if true;
    }
  }
}
